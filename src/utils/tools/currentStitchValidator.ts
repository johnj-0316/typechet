// requires fully functioning row validator and pattern router.
// delay until then.
import { CustomValidator } from "express-validator"

export const currentStitchValidator: CustomValidator = (current_stitch: string) => {

};